# Phase: post-Phase-15 K3.2 (SKS-K3S-10) — security-service base Kustomization.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: security
resources:
  - namespace.yaml
  - security-app-deployment.yaml
  - security-mysql-statefulset.yaml
  - security-mysql-backup-cronjob.yaml
  - network-policies.yaml
  - linkerd-server-authorization.yaml
commonLabels:
  app.kubernetes.io/part-of: workautomations
  workautomations.io/service: security
